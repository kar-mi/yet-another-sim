{
  "name": "yet-another-sim",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun --watch server/server.ts",
    "build": "bun build ./index.html --outdir dist && bun scripts/copy-raids.ts",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@babylonjs/core": "^7.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
