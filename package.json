{
  "name": "yet-another-sim",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun --watch server/server.ts",
    "build": "bun build ./index.html --outdir .bundle",
    "start": "bun server/server.ts",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@babylonjs/core": "^9.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
